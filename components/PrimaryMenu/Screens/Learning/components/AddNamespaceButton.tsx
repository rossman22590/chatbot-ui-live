import { IconPlus } from '@tabler/icons-react';

export const AddNameSpaceButton = () => {
  return (
    <button
      className="flex flex-shrink cursor-pointer items-center gap-3 rounded-md border border-white/20 p-3 text-sm text-white transition-colors duration-200 hover:bg-gray-500/10"
      onClick={() => {}}
    >
      <IconPlus size={16} />
    </button>
  );
};
